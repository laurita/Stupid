import org.scalatest.FunSuite

class PlayerTest extends FunSuite {

  /*
  test("getDefenceCards") {
    val cs
    val (remaining, defenders) = getDefenceCards(cs: List[Card], trump: Suite)
    assert()
  }
  */
}
